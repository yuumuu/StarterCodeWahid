// Project custom scripts (template)
// This file is intended for developers to add project-specific JavaScript.
// System-level UI utilities (skeleton, cache, connection helpers) are provided
// by `core/ui.js` and core styles in `core/styles/core.css`.

// Example: small Alpine helper
/*
document.addEventListener('alpine:init', () => {
  Alpine.data('example', () => ({
    count: 0,
    inc() { this.count++ }
  }));
});
*/

// Example: custom global helper
/*
window.myApp = {
  greet(name) { return `Hello, ${name}` }
};
*/
